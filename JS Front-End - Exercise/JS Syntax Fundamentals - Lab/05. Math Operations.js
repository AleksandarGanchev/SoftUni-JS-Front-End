function mathOperations(firstNumber, secondNumber, operator) {
    if (operator == "+") {
        result = firstNumber + secondNumber;

    } else if (operator == "-") {
        result = firstNumber - secondNumber;

    } else if (operator == "-") {
        result = firstNumber - secondNumber;

    } else if (operator == "*") {
        result = firstNumber * secondNumber;

    } else if (operator == "/") {
        result = firstNumber / secondNumber;

    } else if (operator == "%") {
        result = firstNumber % secondNumber;

    } else if (operator == "**") {
        result = firstNumber ** secondNumber;
    }
    
    console.log(result)
}
